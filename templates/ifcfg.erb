#This is for node <%= node_id %> with public up <%= vip_addr %>
DEVICE=<%= interface_vip %>
HWADDR=<%= vip_addr_mac %>
ONBOOT=yes
BOOTPROTO=static
TYPE=Ethernet
IPADDR=<%= vip_addr %>
<% if vip_addr_gw != "absent" %>
GATEWAY=<%= vip_addr_gw %>
<% end %>
NETMASK=<%= vip_addr_netmask %>
